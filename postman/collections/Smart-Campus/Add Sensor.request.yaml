$kind: http-request
name: Add Sensor
url: "{{baseUrl}}/sensors"
method: POST
body:
  type: json
  content: |-
    {
        "id": "S1",
        "type": "Temperature",
        "roomId": "LAB-1",
        "status": "ACTIVE"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 201", function() {
          pm.response.to.have.status(201);
      });
    language: text/javascript
order: 3344391463781388
